package BoardElements;

/**
 * Enumerator for Rotation.
 * The Enum is using the same convention as the html is using,
 * therefore we are starting from the UP and going clockwise as coming to the LEFT direction.
 */
public enum Rotation {
    UP,
    RIGHT,
    DOWN,
    LEFT,
    NEUTRAL
}
